apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: oppe2-mock-hpa
spec:
  minReplicas: 1
  maxReplicas: 3
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        averageUtilization: 60
        type: Utilization
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: oppe2-mock